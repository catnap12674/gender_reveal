{
  "name": "gender-reveal-app",
  "version": "1.0.0",
  "private": true,
  "description": "スクラッチ演出で性別発表ができるジェンダーリビールWEBアプリ（Cloudflare Pages + KV）",
  "scripts": {
    "dev": "wrangler pages dev public --kv GENDER_KV --binding ADMIN_PASSWORD=change-me-please",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
